query getOrbit($id: ID!) {
  orbit(id: $id) {
    name
    timeframe {
      startTime
      endTime
    }
    metadata {
      description
      isAtomic
    }
    timeframe {
      startTime
      endTime
    }
  }
}
