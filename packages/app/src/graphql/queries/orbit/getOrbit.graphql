query getOrbit($id: ID!) {
  orbit(id: $id) {
    name
    sphereEntryHashB64
    metadata {
      description
      frequency
      scale
    }
    timeframe {
      startTime
      endTime
    }
  }
}
