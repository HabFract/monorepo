query getOrbit($id: ID!) {
  orbit(id: $id) {
    id
    eH
    name
    sphereHash
    frequency
      scale
    metadata {
      description
      timeframe {
        startTime
        endTime
      }
    }
  }
}
