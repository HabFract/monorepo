query getOrbit($id: ID!) {
  orbit(id: $id) {
    name
    sphereHash
    frequency
      scale
    metadata {
      description
      timeframe {
        startTime
        endTime
      }
    }
  }
}
